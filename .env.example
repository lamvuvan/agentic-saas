# ── API ────────────────────────────────────────────────────────────
API_BASE=https://api.openapi.vn/v3

# ── PostgreSQL (analytics DB for BI handler) ────────────────────────────────
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/analytics

# ── Redis (short-term memory / LangGraph checkpointer) ──────────────────────
REDIS_URL=redis://localhost:6379/0

# ── Tool Registry service ────────────────────────────────────────────────────
TOOL_REGISTRY_PORT=8001
TOOLS_YAML_PATH=config/tools.yaml
HOTRELOAD_INTERVAL_S=1

# ── Orchestrator service ─────────────────────────────────────────────────────
ORCHESTRATOR_PORT=8000

# ── Domain Agent services ────────────────────────────────────────────────────
ORDER_AGENT_PORT=8002
BI_AGENT_PORT=8003
CUSTOMER_AGENT_PORT=8004

# ── LLM ──────────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL_FAST=gpt-4o-mini
OPENAI_MODEL_SMART=gpt-4o

# ── Orchestrator A2A routing ─────────────────────────────────────────────────
ORDER_AGENT_URL=http://localhost:8002
BI_AGENT_URL=http://localhost:8003
CUSTOMER_AGENT_URL=http://localhost:8004
TOOL_REGISTRY_URL=http://localhost:8001

# ── Agent Discovery (AgentRegistry) ──────────────────────────────────────────
# Comma-separated base URLs of Domain Agents; Orchestrator fetches Agent Cards
# from {url}/.well-known/agent.json at startup and refreshes every 60s.
# Add new agents here without restarting (takes effect within one refresh cycle).
AGENT_SEED_URLS=http://localhost:8002,http://localhost:8003,http://localhost:8004

# ── A2A polling ──────────────────────────────────────────────────────────────
A2A_POLL_INTERVAL_MS=500
A2A_TIMEOUT_MS=30000

# ── Langfuse (optional — leave blank to disable tracing) ─────────────────────
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=
